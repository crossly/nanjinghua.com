# Domain Docs

This is a single-context repository.

Before changing or specifying a feature, read the root `CONTEXT.md` and the ADRs relevant to the touched area in `docs/adr/`.

Use the glossary vocabulary in specifications, issues, tests, and code. If a proposed change conflicts with an ADR, identify the conflict explicitly rather than silently overriding it.
